'use client'

import styled from 'styled-components';

export const ContainerLayout = styled.body`
 display: flex;
 flex-direction: column;
`;

export const HomeContent = styled.div`
    display: flex;
    flex-direction: column;
    
    width: 100%;
    height: 100%;

    align-items: center;
    justify-content: center;
    justify-items: center;

    gap: 30px;
`;